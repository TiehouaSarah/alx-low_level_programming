#include "main.h"
#include <stdio.h>
/**
 * _strlen - function that gives string length
 *
 * @s : character to give length
 *
 *Return: void
 */

int _strlen(char *s)
{
	int vf = strlen(s);

	return (vf);
}

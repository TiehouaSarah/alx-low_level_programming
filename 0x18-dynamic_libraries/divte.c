#include "main.h"

/**
 * div - divised  two numbers
 *
 *@a: First parameter to be multiply.
 *@b: second parameter to be multiply.
 *
 *Return: params
 */

int div(int a, int b)
{
	if (b != 0)
	{
		return (a / b);
	}
}

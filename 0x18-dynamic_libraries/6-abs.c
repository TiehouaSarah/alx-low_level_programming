#include "main.h"

/**
 *_abs - give the absolute value of a given number
 *
 *@n: The character to be compute.
 *
 *Return: the absolute value of an Integer
 */

int _abs(int n)
{
	if (n > 0)
		return (n);
	else
		return (-n);
}

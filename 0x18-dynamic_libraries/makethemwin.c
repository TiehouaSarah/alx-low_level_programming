#include <stdio.h>
#include <stdlib.h>

/**
 * makethemwin - additionne of two number
 *
 * Return: the resut
 */

int makethemwin(void)
{
	printf("%d %d %d %d %d", 9, 8, 10, 24, 75, 9);
	return (0);
}
